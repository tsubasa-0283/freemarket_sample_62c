= render "toppages/header"
.my-all
  .my-contents
    = render "mypages/my-side"
    .my-logout
      .my-logout__btn
        = link_to users_sign_out_url, method: 'delete', class: "logout-btn" do
          ログアウト
.footer
  = render "toppages/footer"